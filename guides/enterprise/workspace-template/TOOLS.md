# TOOLS.md — Local Environment Notes

## API Toolchain
Script location: `~/.openclaw/workspace/scripts/api-tools.js`

### Available Functions:
- `alpaca_get_price(symbol)` — Stock prices
- `alphavantage_rsi(symbol)` — RSI indicators
- `newsapi_headlines(count)` — News headlines
- `github_list_repos()` — GitHub repositories
- `supabase_query(table, select, limit)` — Database queries
- `stripe_balance()` — Payment balance
- `cloudflare_verify()` — CDN/DNS status
- `vercel_user()` — Deployment info
- `resend_list_keys()` — Email API keys

## TTS
- Preferred voice: [Configure your preferred voice]

## Devices
- [Device name]: [Notes]

## SSH Hosts
- [Host alias]: [IP, user, notes]

---

_Add your environment-specific notes here. Camera names, server IPs, voice preferences — anything unique to your setup._
